async function loadComponent(id,path){const el=document.getElementById(id);if(!el)return;const r=await fetch(path);el.innerHTML=await r.text();}
loadComponent('site-header','/tools/components/header.html');loadComponent('site-footer','/tools/components/footer.html');
const $=s=>document.querySelector(s);
function bind(id,fn){const b=document.getElementById(id);if(b)b.addEventListener('click',fn)}
bind('countWords',()=>{const t=$('#wordInput').value.trim();const words=t?t.split(/\s+/).length:0;$('#wordOutput').textContent=`Words: ${words}\nCharacters: ${t.length}\nCharacters (no spaces): ${t.replace(/\s/g,'').length}`});
bind('convertCase',()=>{const t=$('#caseInput').value;const mode=$('#caseMode').value;const o=mode==='upper'?t.toUpperCase():mode==='lower'?t.toLowerCase():t.replace(/\w\S*/g,w=>w[0].toUpperCase()+w.slice(1).toLowerCase());$('#caseOutput').textContent=o});
bind('removeDupes',()=>{const lines=$('#dupeInput').value.split(/\n/);$('#dupeOutput').textContent=[...new Set(lines)].join('\n')});
bind('calcEMI',()=>{const p=+$('#emiPrincipal').value,r=+$('#emiRate').value/1200,n=+$('#emiMonths').value;const emi=(p*r*Math.pow(1+r,n))/(Math.pow(1+r,n)-1);$('#emiOutput').textContent=isFinite(emi)?`Estimated EMI: ₹${emi.toFixed(2)}`:'Enter valid values';});
bind('calcROI',()=>{const i=+$('#roiInvest').value,f=+$('#roiFinal').value;const roi=((f-i)/i)*100;$('#roiOutput').textContent=isFinite(roi)?`ROI: ${roi.toFixed(2)}%`:'Enter valid values';});
bind('calcPercent',()=>{const n=+$('#pctNumber').value,p=+$('#pctPercent').value;$('#pctOutput').textContent=`${p}% of ${n} = ${(n*p/100).toFixed(2)}`});
bind('genMeta',()=>{const title=$('#metaTitle').value.trim(),desc=$('#metaDesc').value.trim(),kw=$('#metaKeywords').value.trim();$('#metaOutput').textContent=`<title>${title}</title>\n<meta name="description" content="${desc}">\n<meta name="keywords" content="${kw}">`;});
bind('checkDensity',()=>{const t=$('#densityInput').value.toLowerCase().match(/\b[a-z0-9]+\b/g)||[];const total=t.length;const map={};t.forEach(w=>map[w]=(map[w]||0)+1);const sorted=Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,10).map(([w,c])=>`${w}: ${((c/total)*100).toFixed(2)}% (${c})`).join('\n');$('#densityOutput').textContent=sorted||'No words found';});
function readImage(input,cb){const f=input.files[0];if(!f||!f.type.startsWith('image/'))return;const reader=new FileReader();reader.onload=e=>{const img=new Image();img.onload=()=>cb(img,f);img.src=e.target.result};reader.readAsDataURL(f)}
bind('resizeImage',()=>readImage($('#resizeFile'),(img)=>{const w=+$('#resizeWidth').value,h=+$('#resizeHeight').value,c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(img,0,0,w,h);$('#resizePreview').src=c.toDataURL('image/jpeg',.9);$('#resizeDownload').href=$('#resizePreview').src;}));
bind('convertImage',()=>readImage($('#convertFile'),(img)=>{const fmt=$('#convertFormat').value,c=document.createElement('canvas');c.width=img.width;c.height=img.height;c.getContext('2d').drawImage(img,0,0);$('#convertPreview').src=c.toDataURL(`image/${fmt}`,.9);$('#convertDownload').href=$('#convertPreview').src;}));
bind('compressImage',()=>readImage($('#compressFile'),(img)=>{const q=+$('#compressQuality').value/100,c=document.createElement('canvas');c.width=img.width;c.height=img.height;c.getContext('2d').drawImage(img,0,0);$('#compressPreview').src=c.toDataURL('image/jpeg',q);$('#compressDownload').href=$('#compressPreview').src;}));
